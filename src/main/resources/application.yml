server:
  port: 8080

spring:
  application:
    name: there

  datasource:
    url: jdbc:mysql://there-db.cjg8pisspmbh.ap-northeast-2.rds.amazonaws.com:3306/There
    username: There
    password: therezzang!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: ture

    logging:
      level:
        org:
          hibernate:
            type:
              descriptor:
                sql: trace

    sql:
      init:
        platform: mysql

  sql:
    init:
      platform: mysql

  redis:
    port: 6379
    host: localhost

  jwt:
    secret-key: SECRET_KEY
    blacklist:
      access-token: BlackList_AccessToken_

cloud:
  aws:
    credentials:
      access-key: AKIAVAK5CW4BP5NVWN7X
      secret-key: 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
    region:
      static: ap-northeast-2
    s3:
      bucket: there-s3-bucket
    stack:
      auto: false